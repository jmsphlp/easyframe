{
  "name": "easyframe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Add clean borders to photos and videos without cropping. PWA, runs entirely on-device.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
